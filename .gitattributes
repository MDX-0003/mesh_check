# 换行符一律不做转换（按字节检出/入库）。
#
# 为什么：渲染与交付层大量依赖**产物字节**与 mtime 判据——
#   1) 交付页/图证的转码跳过判据看的是 mtime，JSONL/HTML 参与逐字节比对；
#   2) 验收里有一条"用数据包重建出的快照与仓库里那份逐字节一致"（diff -rq 无输出）。
# 2026-09-23 实测踩到：本机 `core.autocrlf` 把 checkout 出来的 jsonl 转成 CRLF，
# 于是重建出的 `prompts.jsonl` 与入库那份差了 20 个 \r（内容相同、字节不同），
# 让"逐字节一致"这条验收出现假失败。
#
# 统一 `-text`：Git 不碰任何文件的换行符，跨平台（Windows/macOS/Linux）行为一致。
* -text
